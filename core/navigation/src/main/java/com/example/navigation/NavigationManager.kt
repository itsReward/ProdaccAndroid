package com.example.navigation

import android.content.Context
import androidx.navigation.NavController
import androidx.navigation.NavHostController
//import com.example.auth.domain.UserManager

/*
class NavigationManager(
    private val userManager: UserManager,
    private val navController: NavController, context: Context
): NavHostController(context)
{
    fun navigateToLogIn() {
        navController.navigate(Route.LogIn.path)
    }

    fun navigateToJobCards() {
        if (userManager.isLoggedIn) {
            navController.navigate(Route.JobCards.path)
        } else {
            // Navigate to login screen or show error
        }
    }

    fun navigateToVehicles() {
        if (userManager.isLoggedIn) {
            navController.navigate(Route.Vehicles.path)
        } else {
            // Navigate to login screen or show error
        }
    }

    fun navigateToEmployees() {
        if (userManager.isLoggedIn) {
            navController.navigate(Route.Employees.path)
        } else {
            // Navigate to login screen or show error
        }
    }

    fun navigateToClients() {
        if (userManager.isLoggedIn && userManager.isAdmin) {
            navController.navigate(Route.Clients.path)
        } else {
            // Show error or navigate to unauthorized screen
        }
    }
}*/
